# Https调试方案：在本地hosts文件中添加映射

### 步骤1：编辑hosts文件

1. 以管理员身份打开记事本
   - 按 `Win + R`，输入 `notepad`
   - 右键选择"以管理员身份运行"
2. 打开hosts文件
   - 在记事本中按 `Ctrl + O`
   - 导航到：`C:\Windows\System32\drivers\etc\hosts`
   - 选择"所有文件(*.*)"才能看到hosts文件

### 步骤2：添加域名映射

在文件末尾添加这行：

plaintext

```
192.168.1.7  www.autosquare.org.cn
```

### 步骤3：保存文件

- 按 `Ctrl + S` 保存
- 如果提示权限问题，确认以管理员身份保存

### 步骤4：刷新DNS缓存

打开命令提示符（管理员）执行：

cmd

```
ipconfig /flushdns
```

## 🧪 验证配置

### 测试方法：

1. 打开浏览器
2. 访问 `https://www.autosquare.org.cn`
3. 应该能正常显示你的网站，且地址栏显示安全锁图标

### 验证hosts是否生效：

在命令提示符中执行：

cmd

```
ping www.autosquare.org.cn
```

应该返回 `192.168.1.7` 的IP地址

## 💡 注意事项

- **临时性**：这只是本地开发解决方案
- **团队协作**：如果其他人也要访问，他们也需要配置自己的hosts文件
- **生产环境**：正式部署时需要用真实的域名解析

这样配置后，你就能在本地用域名正常访问HTTPS网站了！